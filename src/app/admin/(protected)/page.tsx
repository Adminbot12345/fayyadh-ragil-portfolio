import { getAdminProjects } from '@/features/projects/repository'
export default async function AdminOverview(){const projects=await getAdminProjects();const published=projects.filter(p=>p.isPublished).length,drafts=projects.length-published,featured=projects.filter(p=>p.isFeatured).length,recent=[...projects].sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0,5);return <div><p className="eyebrow">Overview</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.04em]">Portfolio dashboard</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Total',projects.length],['Published',published],['Drafts',drafts],['Featured',featured]].map(([l,v])=><div key={l} className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><p className="text-sm text-white/40">{l}</p><p className="mt-3 text-4xl font-semibold">{v}</p></div>)}</div><div className="mt-10"><h2 className="text-xl font-semibold">Recently updated</h2><div className="mt-4 divide-y divide-white/10 rounded-2xl border border-white/10">{recent.map(p=><a href={`/admin/projects/${p.id}/edit`} key={p.id} className="flex items-center justify-between p-4"><span>{p.title}</span><span className="text-xs text-white/35">{p.isPublished?'Published':'Draft'} →</span></a>)}</div></div></div>}
